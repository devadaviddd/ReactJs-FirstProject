import React, {CSSProperties, ReactElement, RefObject, useContext} from "react";
import '../../index.css';

export const Home: React.FC<any> = (props, context): ReactElement => {

  return (
    <div>
      This is default home page
    </div>
  );
}